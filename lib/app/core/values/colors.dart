import 'package:flutter/material.dart'; // นำเข้าแพ็คเกจ material ของ Flutter

const purple = Color(0xFF756BFC); // กำหนดสีม่วง
const pink = Color(0xFFF1A39A); // กำหนดสีชมพู
const deepPink = Color(0xFFFA63C6); // กำหนดสีชมพูเข้ม
const green = Color(0xFF41CF9F); // กำหนดสีเขียว
const yellow = Color(0xFFEEC38E); // กำหนดสีเหลือง
const lightBlue = Color(0xFF42A5F5); // กำหนดสีฟ้าอ่อน
const blue = Color(0xFF2B60E6); // กำหนดสีน้ำเงิน
